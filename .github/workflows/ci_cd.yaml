name: CI-CD

on: push

jobs:
  CI-CD:
    runs-on: ubuntu-latest
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install dvc[s3]
          pip install pytest

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: eu-north-1

      - name: DVC Pull
        run: |
          dvc pull

      - name: Test Model Registry
        env:
          DAGSHUB_USER_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          pytest tests/test_model_registry.py -v

      - name: Test Model Performance
        env:
          DAGSHUB_USER_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          pytest tests/test_model_perf.py -v

      - name: Promote Model to Production
        if: success()
        env:
          DAGSHUB_USER_TOKEN: ${{ secrets.DAGSHUB_TOKEN }}
        run: |
          python scripts/promote_model_to_prod.py

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push docker image to Amazon ECR
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: swiggy-delivery
          IMAGE_TAG: latest
        run: |
          docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG .
          docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG

      # ----------------------------
      # CodeDeploy steps - commented out for now
      # ----------------------------

      # - name: Zip files for deployment
      #   if: success()
      #   run: |
      #     zip -r deployment.zip appspec.yml deploy/scripts/install_dependencies.sh deploy/scripts/start_docker.sh

      # - name: Upload ZIP to S3
      #   if: success()
      #   run: |
      #     aws s3 cp deployment.zip s3://food-delivery-time-predcn-project-bucket/deployment.zip

      # - name: Deploy to AWS CodeDeploy
      #   if: success()
      #   run: |
      #     aws deploy create-deployment \
      #     --application-name <YOUR_APP_NAME> \
      #     --deployment-config-name CodeDeployDefault.OneAtATime \
      #     --deployment-group-name <YOUR_DEPLOYMENT_GROUP_NAME> \
      #     --s3-location bucket=food-delivery-time-predcn-project-bucket,key=deployment.zip,bundleType=zip \
      #     --file-exists-behavior OVERWRITE \
      #     --region eu-north-1